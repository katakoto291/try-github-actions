name: Production Workflow

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      branch:
        required: true
        type: string

jobs:
  prod-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - run: echo "Running PROD workflow for ${{ inputs.branch }}"
